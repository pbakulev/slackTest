pipeline{
    agent any
    stages {
    
        stage('Stage'){
            steps{
            
                script{
                    multibranchPipelineJob('Example') {
                        configure { project ->
                            ( project / 'authToken' ).setValue('value')
                        }
                    }
                }
            
            }
            
        }
    
    }
    
}

