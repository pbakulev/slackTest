pipeline{
    agent any
    stages {
    
        stage('Stage'){
            steps{
            
                script{
                    jobDsl scriptText:
                    """multibranchPipelineJob('Example') {
                        configure { project ->
                            ( project / 'authToken' ).setValue('value')
                        }
                        """
                    }
                }
            
            }
            
        }
    
    }
    
}

